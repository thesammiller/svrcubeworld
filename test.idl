interface Simple_Server
{
  typedef float HeadPoseArray[7];

  oneway void send_data(in long microsecond, in HeadPoseArray headpose);

  typedef sequence<octet, 200000> pixels;

  pixels sendImageData();

  long sendJpegSize();

};
